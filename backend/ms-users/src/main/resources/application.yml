spring:
  application:
    name: ms-users
  # configuracion de la base de datos
  datasource:
    url: jdbc:mysql://localhost:3306/obertibussoserviciosinmobiliarios
    username: root
    password: lmcpauli1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    jpa:
      hibernate:
        ddl-auto: create-drop
      show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/obertibussoserviciosinmobiliarios

server:
  port: 8081

eureka:
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

pi:
  keycloak:
    serverUrl: http://localhost:8080/
    realm: obertibussoserviciosinmobiliarios
    clientId: springboot-keycloak-client
    clientSecret: ZCPO91FLkzqGVkDKBz3cAgihJjyb19Ub

email:
  username: desarrolloinmobertibusso@gmail.com
  password: gian ovac cerv btzf

frontend:
  base-url: http://localhost:5173

client:
  secret: qI8ZfJH8VnL2bBt7T4z0c91wWoTQUxAWm6q1fXKhfH4=

property:
  service:
    url: http://localhost:8081

---

# Configuración para perfil "test"
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop