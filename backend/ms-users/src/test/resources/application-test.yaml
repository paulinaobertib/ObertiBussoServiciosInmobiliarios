spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: create-drop

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost/mock

server:
  port: 0

eureka:
  client:
    enabled: false
  instance:
    enabled: false

pi:
  keycloak:
    serverUrl:   http://localhost/mock
    realm:       dummy
    clientId:    dummy
    clientSecret: dummy

email:
  username: test
  password: test

frontend:
  base-url: http://localhost