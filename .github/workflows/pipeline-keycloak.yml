name: CI/CD - Keycloak

on:
  push:
    branches: [feature/pipeline]
  workflow_dispatch:

# --------------------------------------------------------------
# VARIABLES GLOBALES
# --------------------------------------------------------------
env:
  # Azure
  AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
  ACA_ENVIRONMENT: ${{ secrets.ACA_ENVIRONMENT }}
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  AZURE_RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}

  # Docker Hub
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  # Health-check
  HEALTH_PATH: /actuator/health

  # Gateway-specific
  IMAGE_NAME: keycloak
  ACA_KEYCLOAK_DEV: ${{ vars.ACA_KEYCLOAK_DEV }}
  ACA_KEYCLOAK_PROD: ${{ vars.ACA_KEYCLOAK_PROD }}
  REALM_FILE_PATH: ./realm/realm-obertibussoserviciosinmobiliarios.json
  SERVICE_PORT: 8080

jobs:
  # --------------------------------------------------------------
  # JOB 1: Build · Test · Docker
  # --------------------------------------------------------------
  build-and-push:
    name: Build · Test · Docker
    runs-on: ubuntu-latest
    environment: development

    outputs:
      tag: ${{ steps.build-image.outputs.tag }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Build & Push Docker Image
        id: build-image
        run: |
          TAG=${GITHUB_SHA::8}
          IMAGE="docker.io/${{env.DOCKERHUB_USERNAME}}/${{ env.IMAGE_NAME }}:$TAG"
          docker build --no-cache -t $IMAGE -f ${{ env.IMAGE_NAME }}/Dockerfile .
          docker push $IMAGE
          echo "tag=$TAG" >> $GITHUB_OUTPUT

  # --------------------------------------------------------------
  # JOB 2: Deploy to Development
  # --------------------------------------------------------------
  deploy-dev:
    name: Deploy to Development
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: development

    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ env.AZURE_CREDENTIALS }}
          enable-AzPSSession: true

      - name: Ensure Container App Exists
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            TAG=${{ needs.build-and-push.outputs.tag }}
            IMAGE="docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:$TAG"

            if ! az containerapp show -g $AZURE_RESOURCE_GROUP -n $ACA_KEYCLOAK_DEV &>/dev/null; then
              echo "🚀 Creating $ACA_KEYCLOAK_DEV..."
              az containerapp create \
                -g $AZURE_RESOURCE_GROUP \
                -n $ACA_KEYCLOAK_DEV \
                --environment $ACA_ENVIRONMENT \
                --image $IMAGE \
                --ingress external \
                --target-port $SERVICE_PORT \
                --registry-server docker.io \
                --registry-username $DOCKERHUB_USERNAME \
                --registry-password $DOCKERHUB_TOKEN \
                --cpu 1.5 \
                --memory 3Gi \
                --env-vars KC_BOOTSTRAP_ADMIN_USERNAME=${{ secrets.KEYCLOAK_ADMIN }} KC_BOOTSTRAP_ADMIN_PASSWORD=${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}

            else
              echo "✏️ $ACA_KEYCLOAK_DEV already exists."
            fi

      - name: Ensure Container App Is Running
        uses: azure/powershell@v1
        with:
          azPSVersion: latest
          inlineScript: |
            # Leemos el estado actual
            $status = (az containerapp show `
              --resource-group $Env:AZURE_RESOURCE_GROUP `
              --name $Env:ACA_KEYCLOAK_DEV `
              --query properties.runningStatus `
              -o tsv).Trim()
            Write-Host "Current runningStatus (via CLI): $status"

            if ($status -ne 'Running') {
              Write-Host "🔄 Starting Container App via Start-AzContainerApp..."
              Start-AzContainerApp `
                -Name $Env:ACA_KEYCLOAK_DEV `
                -ResourceGroupName $Env:AZURE_RESOURCE_GROUP `
                -SubscriptionId $Env:AZURE_SUBSCRIPTION_ID
            }
            else {
              Write-Host "✅ Container App is already Running."
            }

      - name: Fetch Keycloak FQDN
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            FQDN=$(az containerapp show \
              -g ${{ env.AZURE_RESOURCE_GROUP }} \
              -n ${{ vars.ACA_KEYCLOAK_DEV }} \
              --query properties.configuration.ingress.fqdn -o tsv)
            echo "🔖 Keycloak FQDN: $FQDN"
            echo "KEYCLOAK_HOSTNAME=$FQDN" >> $GITHUB_ENV

      - name: Update Image & Scale
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            TAG=${{ needs.build-and-push.outputs.tag }}
            IMAGE="docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:$TAG"

            FQDN=$(az containerapp show -g $AZURE_RESOURCE_GROUP -n $ACA_KEYCLOAK_DEV --query properties.configuration.ingress.fqdn -o tsv)


            echo "🔄 Updating image to $IMAGE and setting min-replicas=1..."
            az containerapp update \
              --name $ACA_KEYCLOAK_DEV \
              --resource-group $AZURE_RESOURCE_GROUP \
              --image $IMAGE \
              --min-replicas 1 \
              --set-env-vars \
                KC_HOSTNAME=$FQDN
                KC_BOOTSTRAP_ADMIN_USERNAME="${{ secrets.KEYCLOAK_ADMIN }}" \
                KC_BOOTSTRAP_ADMIN_PASSWORD="${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}" \

      - name: Restart to apply Changes
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            # Obtenemos la última revisión lista (lista y lista de nuevo)
            REVISION=$(az containerapp show \
              -g $AZURE_RESOURCE_GROUP \
              -n $ACA_KEYCLOAK_DEV \
              --query properties.latestReadyRevisionName \
              -o tsv)

            if [[ -n "$REVISION" ]]; then
              echo "🔄 Restarting revision $REVISION..."
              az containerapp revision restart \
                -g $AZURE_RESOURCE_GROUP \
                -n $ACA_KEYCLOAK_DEV \
                --revision "$REVISION"
            else
              echo "ℹ️ No ready revision found, skipping restart."
            fi

      - name: Health Check Keycloak
        run: |
          URL="https://$KEYCLOAK_HOSTNAME/realms/obertibussoserviciosinmobiliarios/.well-known/openid-configuration"
          echo "🩺 Checking health at $URL"
          for i in $(seq 1 10); do
            HTTP=$(curl -sk -o /dev/null -w "%{http_code}" $URL) || true
            if [[ "$HTTP" -ge 200 && "$HTTP" -lt 300 ]]; then
              echo "✅ Keycloak is healthy"
              exit 0
            fi
            echo "⏳ Attempt $i failed (status $HTTP), retrying in 10s..."
            sleep 10
          done
          echo "❌ Keycloak failed healthcheck"
          exit 1

  # --------------------------------------------------------------
  # JOB 3: Deploy to Production
  # --------------------------------------------------------------
  deploy-prod:
    name: Deploy to Production
    needs: [deploy-dev, build-and-push]
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ env.AZURE_CREDENTIALS }}
          enable-AzPSSession: true

      - name: Ensure Container App Exists
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            TAG=${{ needs.build-and-push.outputs.tag }}
            IMAGE="docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:$TAG"

            if ! az containerapp show -g $AZURE_RESOURCE_GROUP -n $ACA_KEYCLOAK_PROD &>/dev/null; then
              echo "🚀 Creating $ACA_KEYCLOAK_PROD..."
              az containerapp create \
                -g $AZURE_RESOURCE_GROUP \
                -n $ACA_KEYCLOAK_PROD \
                --environment $ACA_ENVIRONMENT \
                --image $IMAGE \
                --ingress external \
                --target-port $SERVICE_PORT \
                --registry-server docker.io \
                --registry-username $DOCKERHUB_USERNAME \
                --registry-password $DOCKERHUB_TOKEN \
                --cpu 1.5 \
                --memory 3Gi \
                --env-vars KC_BOOTSTRAP_ADMIN_USERNAME=${{ secrets.KEYCLOAK_ADMIN }} KC_BOOTSTRAP_ADMIN_PASSWORD=${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}

            else
              echo "✏️ $ACA_KEYCLOAK_PROD already exists."
            fi

      - name: Ensure Container App Is Running
        uses: azure/powershell@v1
        with:
          azPSVersion: latest
          inlineScript: |
            # Leemos el estado actual
            $status = (az containerapp show `
              --resource-group $Env:AZURE_RESOURCE_GROUP `
              --name $Env:ACA_KEYCLOAK_PROD `
              --query properties.runningStatus `
              -o tsv).Trim()
            Write-Host "Current runningStatus (via CLI): $status"

            if ($status -ne 'Running') {
              Write-Host "🔄 Starting Container App via Start-AzContainerApp..."
              Start-AzContainerApp `
                -Name $Env:ACA_KEYCLOAK_PROD `
                -ResourceGroupName $Env:AZURE_RESOURCE_GROUP `
                -SubscriptionId $Env:AZURE_SUBSCRIPTION_ID
            }
            else {
              Write-Host "✅ Container App is already Running."
            }

      - name: Fetch Keycloak FQDN
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            FQDN=$(az containerapp show \
              -g ${{ env.AZURE_RESOURCE_GROUP }} \
              -n ${{ vars.ACA_KEYCLOAK_PROD }} \
              --query properties.configuration.ingress.fqdn -o tsv)
            echo "🔖 Keycloak FQDN: $FQDN"
            echo "KEYCLOAK_HOSTNAME=$FQDN" >> $GITHUB_ENV

            echo $KEYCLOAK_HOSTNAME

      - name: Update Image & Scale
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            TAG=${{ needs.build-and-push.outputs.tag }}
            IMAGE="docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:$TAG"

            echo "🔄 Updating image to $IMAGE and setting min-replicas=1..."
            az containerapp update \
              --name $ACA_KEYCLOAK_PROD \
              --resource-group $AZURE_RESOURCE_GROUP \
              --image $IMAGE \
              --min-replicas 1 \
              --set-env-vars \
                KC_BOOTSTRAP_ADMIN_USERNAME="${{ secrets.KEYCLOAK_ADMIN }}" \
                KC_BOOTSTRAP_ADMIN_PASSWORD="${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}" \

      - name: Restart to apply Changes
        uses: azure/cli@v1
        with:
          azcliversion: latest
          inlineScript: |
            set -e
            # Obtenemos la última revisión lista (lista y lista de nuevo)
            REVISION=$(az containerapp show \
              -g $AZURE_RESOURCE_GROUP \
              -n $ACA_KEYCLOAK_PROD \
              --query properties.latestReadyRevisionName \
              -o tsv)

            if [[ -n "$REVISION" ]]; then
              echo "🔄 Restarting revision $REVISION..."
              az containerapp revision restart \
                -g $AZURE_RESOURCE_GROUP \
                -n $ACA_KEYCLOAK_PROD \
                --revision "$REVISION"
            else
              echo "ℹ️ No ready revision found, skipping restart."
            fi

      - name: Health Check Keycloak
        run: |
          URL="https://$KEYCLOAK_HOSTNAME/realms/obertibussoserviciosinmobiliarios/.well-known/openid-configuration"
          echo "🩺 Checking health at $URL"
          for i in $(seq 1 10); do
            HTTP=$(curl -sk -o /dev/null -w "%{http_code}" $URL) || true
            if [[ "$HTTP" -ge 200 && "$HTTP" -lt 300 ]]; then
              echo "✅ Keycloak is healthy"
              exit 0
            fi
            sleep 5
          done
          echo "❌ Keycloak failed healthcheck"
          exit 1
